var main = function(){

var countsJSON = require("./counts.json"),
    counts = {},
    userChoice = prompt("Lets play a game! Ok... 'Heads' or 'Tails' ? Type your choice.");

};

$(document).ready(main);
